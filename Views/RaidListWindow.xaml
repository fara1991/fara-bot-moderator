<Window
    x:Class="FaraBotModerator.Views.RaidListWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FaraBotModeratorRaidList"
    Height="600"
    Width="660"
    Icon="pack://application:,,,/resources/app.ico"
    ResizeMode="CanMinimize">
    <Window.Resources>
        <Style TargetType="GroupBox" BasedOn="{StaticResource {x:Type GroupBox}}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <ContentPresenter Content="{Binding}" Margin="8,0,0,0" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <GroupBox Height="600" Width="660" HorizontalAlignment="Center"
              VerticalAlignment="Top" TextBlock.Foreground="Black">
        <GroupBox.Header>
            <TextBlock Text="FaraBotTwitchRaidList" Foreground="White" />
        </GroupBox.Header>
        <Canvas Height="550" Width="640">
            <DataGrid x:Name="TwitchRaidDataGrid"
                      Height="460"
                      Width="640"
                      HeadersVisibility="Column"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserResizeRows="True"
                      CanUserResizeColumns="False"
                      CanUserSortColumns="True"
                      AlternatingRowBackground="MediumPurple"
                      AlternationCount="2"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      EnableColumnVirtualization="True"
                      EnableRowVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Icon" IsReadOnly="True" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Icon}" Width="48" Height="48" VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!-- 表示はしないがレイドコマンド用に保持する -->
                    <DataGridTextColumn Header="LoginId" IsReadOnly="True" Visibility="Hidden"
                                        Binding="{Binding LoginId}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="TextWrapping" Value="Wrap" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Name" IsReadOnly="True" Width="120" Binding="{Binding Name}"
                                        EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="TextWrapping" Value="NoWrap" />
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Game" IsReadOnly="True" Width="160" Binding="{Binding GameName}"
                                        EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnEditingStyle}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="TextWrapping" Value="NoWrap" />
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="StartedAt" IsReadOnly="True" Width="120"
                                        Binding="{Binding StartedAt, StringFormat=\{0:MM/dd HH:mm:ss\}}"
                                        EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnPopupEditingStyle}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Viewer" IsReadOnly="True" Width="70" Binding="{Binding Viewer}"
                                        EditingElementStyle="{StaticResource MaterialDesignDataGridTextColumnPopupEditingStyle}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="HorizontalAlignment" Value="Right" />
                                <Setter Property="Margin" Value="0,0,10,0" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="Raid" IsReadOnly="True" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Button Click="DataGridRowRaidButton_OnClick" Content="Go" Width="60"
                                            Height="32"
                                            x:Name="RaidButton" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    <Popup x:Name="CopyPopup"
                                           PlacementTarget="{Binding ElementName=RaidButton}"
                                           Placement="Bottom"
                                           PopupAnimation="Fade"
                                           AllowsTransparency="True">
                                        <Border Background="#FF333333"
                                                CornerRadius="5"
                                                Padding="10,5">
                                            <TextBlock Text="コピーしました"
                                                       Foreground="White" />
                                        </Border>
                                    </Popup>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Button Content="StreamingFollower" VerticalAlignment="Top"
                    Canvas.Left="125"
                    Canvas.Top="470" HorizontalAlignment="Left" Width="180" Height="35"
                    Click="TwitchStreamingFollowerButton_OnClick" />
            <Button Content="StreamingSameGame" VerticalAlignment="Top"
                    Canvas.Left="345"
                    Canvas.Top="470" HorizontalAlignment="Left" Width="180" Height="35"
                    Click="TwitchStreamingSameGameButton_OnClick" />
        </Canvas>
    </GroupBox>
</Window>