# FaraBotModerator 操作説明書

このドキュメントでは、FaraBotModeratorの初期設定から各機能の使い方までを解説します。

---

## 目次

1. [はじめに](#1-はじめに)
2. [動作環境・必要条件](#2-動作環境必要条件)
3. [初期設定（Main Settings）](#3-初期設定main-settings)
4. [リアクションイベント設定（Reaction Event Settings）](#4-リアクションイベント設定reaction-event-settings)
5. [自動ボット設定（Auto Bot Settings）](#5-自動ボット設定auto-bot-settings)
6. [ウィンドウ設定（Windows Settings）](#6-ウィンドウ設定windows-settings)
7. [よくある質問（FAQ）](#7-よくある質問faq)
8. [トラブルシューティング](#8-トラブルシューティング)

---

## 1. はじめに

FaraBotModeratorは、Twitchストリーマー向けの多機能チャットボットアプリケーションです。

### 主な機能
- **チャット管理**: Twitchチャットの表示・送信
- **自動翻訳**: DeepLを使用した日英翻訳
- **読み上げ連携**: 棒読みちゃんとの連携
- **イベント通知**: フォロー、レイド、サブスクなどの自動応答
- **定期メッセージ**: タイマーによる自動メッセージ送信
- **レイド支援**: 配信終了時のレイド先候補表示

### 起動方法
インストール後、デスクトップのショートカットまたはインストール先の `FaraBotModerator.exe` を実行してください。

---

## 2. 動作環境・必要条件

### 必須ソフトウェア
- **Windows 10/11**
- **.NET 8.0 ランタイム**
- **WebView2 ランタイム**（Microsoft Edge ベース）

### 必要なアカウント・API
- **Twitchアカウント**
- **Twitch Developer アプリケーション**（API連携用）
- **DeepL APIキー**（翻訳機能を使用する場合）
- **棒読みちゃん**（読み上げ機能を使用する場合）

---

## 3. 初期設定（Main Settings）

アプリを起動すると「Main Settings」タブが表示されます。Twitchとの連携設定を行います。

### 3.1. Client設定

Twitchチャットクライアントの基本設定です。

| 項目 | 説明 |
|------|------|
| **UserName** | Twitchにログインするときのユーザー名（URLの末尾の名前） |
| **DisplayName** | Twitchで表示される名前 |

### 3.2. API設定

Twitch APIとの連携設定です。

#### Twitch Developer アプリケーションの作成手順

1. [Twitch Developer Console](https://dev.twitch.tv/console) にアクセス
2. 「アプリケーションを登録」をクリック
3. 以下の情報を入力：
   - **名前**: 任意のアプリ名
   - **OAuth リダイレクト URL**: `http://localhost:3000`（ポート番号は設定に合わせる）
   - **カテゴリ**: 「Chat Bot」を選択
4. 作成後、「Client ID」と「Client Secret」が発行されます

#### 設定項目

| 項目 | 説明 |
|------|------|
| **ClientId** | Twitch Developerで発行されたClient ID |
| **ClientSecret** | Twitch Developerで発行されたClient Secret |

#### 認証手順

1. ClientIdとClientSecretを入力
2. 「**Authorize**」ボタンをクリック
3. 右側のWebViewにTwitchログイン画面が表示される
4. Twitchアカウントでログインし、アプリを許可
5. 認証完了後、Token expirationに有効期限が表示される

### 3.3. 棒読みちゃん連携

チャットの読み上げ機能を使用する場合の設定です。

1. 棒読みちゃんを起動
2. 棒読みちゃんの設定で「HTTP連携」を有効化（ポート: 5008）
3. アプリ側で「**BouyomiChanConnect**」にチェック

### 3.4. 接続・切断

| ボタン | 説明 |
|--------|------|
| **TwitchPage** | WebViewにTwitchチャンネルページを表示 |
| **Connect** | Twitchチャットに接続 |
| **Disconnect** | Twitchチャットから切断 |

接続状態は「**State: Connect / Disconnect**」で確認できます。

---

## 4. リアクションイベント設定（Reaction Event Settings）

「Reaction Event Settings」タブでは、各種Twitchイベント発生時に自動送信するメッセージを設定できます。

### 4.1. 対応イベント一覧

| イベント | 説明 |
|----------|------|
| **Follow** | 誰かがチャンネルをフォローした時 |
| **Raid** | 他の配信者からレイドされた時 |
| **Subscription** | 誰かがサブスクライブした時 |
| **Bits** | 誰かがビッツ（チアー）を投げた時 |
| **Gift** | 誰かがサブスクギフトを贈った時 |
| **ChannelPoint** | 誰かがチャンネルポイントを使用した時 |

### 4.2. 設定方法

各イベントごとに以下の設定ができます：

1. **チェックボックス**: イベント通知のON/OFF
2. **メッセージ欄**: 送信するメッセージのテンプレート
3. **Test Send**: テストメッセージを送信（接続中のみ有効）

### 4.3. プレースホルダー（置換文字）

メッセージ内で使用できる特殊な文字列です。イベント発生時に実際の値に置き換えられます。

#### Follow
| プレースホルダー | 説明 |
|------------------|------|
| `{followerName}` | フォローしたユーザーの名前 |
| `{followerChannelUrl}` | フォローしたユーザーのチャンネルURL |

#### Raid
| プレースホルダー | 説明 |
|------------------|------|
| `{raiderName}` | レイドしてきたユーザーの名前 |
| `{raiderChannelUrl}` | レイドしてきたユーザーのチャンネルURL |

#### Subscription
| プレースホルダー | 説明 |
|------------------|------|
| `{subscriberName}` | サブスクしたユーザーの名前 |
| `{totalSubscriptionMonth}` | 累計サブスク月数 |

#### Bits
| プレースホルダー | 説明 |
|------------------|------|
| `{bitsSendUserName}` | ビッツを投げたユーザーの名前 |
| `{bitsAmount}` | 投げられたビッツの量 |

#### Gift
| プレースホルダー | 説明 |
|------------------|------|
| `{giftedUserName}` | ギフトを贈ったユーザーの名前 |

#### ChannelPoint
| プレースホルダー | 説明 |
|------------------|------|
| `{channelPointUserName}` | チャンネルポイントを使用したユーザーの名前 |
| `{channelPointTitle}` | 使用されたチャンネルポイント報酬のタイトル |
| `{channelPointCost}` | 消費されたポイント数 |

### 4.4. 設定例

```
Follow: {followerName}さん、フォローありがとうございます！
Raid: {raiderName}さん、レイドありがとうございます！チャンネル: {raiderChannelUrl}
Subscription: {subscriberName}さん、{totalSubscriptionMonth}ヶ月目のサブスクありがとうございます！
Bits: {bitsSendUserName}さん、{bitsAmount}ビッツありがとうございます！
```

---

## 5. 自動ボット設定（Auto Bot Settings）

「Auto Bot Settings」タブでは、翻訳機能とタイマー機能を設定できます。

### 5.1. 翻訳設定（Translate）

DeepL APIを使用した自動翻訳機能の設定です。

#### DeepL APIキーの取得方法

1. [DeepL](https://www.deepl.com/) にアクセス
2. アカウントを作成（無料プランあり）
3. マイページから「APIキー」を取得

#### 設定方法

1. 「**DeepL API Key**」欄にAPIキーを入力
2. 「**DeepL Site**」ボタンでDeepLサイトをWebViewに表示可能

#### 翻訳の動作

- チャットメッセージを自動検出
- 日本語 → 英語、英語 → 日本語に自動翻訳
- 翻訳結果をチャットに送信

### 5.2. タイマー設定（Timer）

定期的または指定時刻にメッセージを自動送信する機能です。

#### Cycle Notifications（定期メッセージ）

指定した間隔で繰り返しメッセージを送信します。

| 設定項目 | 説明 |
|----------|------|
| **チェックボックス** | タイマーのON/OFF |
| **スライダー** | 送信間隔（15分～120分） |
| **メッセージ欄** | 送信するメッセージ |

4つのタイマー（CycleTimer1～4）を設定可能です。

**使用例**:
- 配信ルールの定期告知
- SNSアカウントの宣伝
- Discordサーバーへの招待

#### Fixed Notifications（日時指定メッセージ）

指定した日時にメッセージを1回送信します。

| 設定項目 | 説明 |
|----------|------|
| **チェックボックス** | タイマーのON/OFF |
| **日付** | 送信する日付 |
| **時刻** | 送信する時刻（秒単位まで指定可能） |
| **メッセージ欄** | 送信するメッセージ |

4つのタイマー（FixedTimer1～4）を設定可能です。

**使用例**:
- 配信終了予告
- イベント開始告知
- 特定時刻のお知らせ

---

## 6. ウィンドウ設定（Windows Settings）

「Windows Settings」タブでは、補助ウィンドウを管理できます。

### 6.1. Chat Window（チャットウィンドウ）

Twitchチャットをリアルタイムで表示・送信するウィンドウです。

**機能**:
- チャットメッセージの一覧表示
- ユーザーアイコンの表示
- メッセージの送信

「**Open Chat Window**」ボタンで開きます。

### 6.2. Raid List Window（レイドリストウィンドウ）

配信終了時のレイド先候補を表示するウィンドウです。

**機能**:
- 現在配信中のフォロー中ユーザーを表示
- 同じゲームをプレイ中の配信者を表示
- レイドコマンドをクリップボードにコピー

「**Open Raid List Window**」ボタンで開きます。

---

## 7. よくある質問（FAQ）

### Q. 起動時にエラーが発生します
**A.** 以下を確認してください：
- .NET 8.0 ランタイムがインストールされているか
- WebView2 ランタイムがインストールされているか
- ウイルス対策ソフトにブロックされていないか

### Q. チャットが取得できません
**A.** 以下を確認してください：
- AccessTokenの有効期限が切れていないか（Main Settingsで確認）
- 「Authorize」ボタンから再認証を試す
- UserNameが正しく設定されているか

### Q. 翻訳が動作しません
**A.** 以下を確認してください：
- DeepL APIキーが正しく入力されているか
- DeepL APIの月間使用量が上限に達していないか
- インターネット接続が正常か

### Q. 棒読みちゃんで読み上げられません
**A.** 以下を確認してください：
- 棒読みちゃんが起動しているか
- 棒読みちゃんのHTTP連携が有効か（ポート5008）
- 「BouyomiChanConnect」にチェックが入っているか
- Twitchに接続する前に棒読みちゃんを起動しているか

### Q. イベント通知が届きません
**A.** 以下を確認してください：
- 各イベントのチェックボックスがONになっているか
- Twitchに正常に接続されているか（State: Connect）
- 「Test Send」ボタンでテスト送信を試す

### Q. 設定が保存されません
**A.** アプリ終了時に自動保存されます。設定は `Resources\secrets.json` ファイルに保存されます。

---

## 8. トラブルシューティング

### Token期限切れエラー

「Your API AccessToken has expired」と表示された場合：

1. 「**Authorize**」ボタンをクリック
2. WebViewでTwitchにログイン
3. アプリを許可
4. Token expirationが更新されることを確認

### 接続が不安定な場合

1. 一度「**Disconnect**」で切断
2. 数秒待ってから「**Connect**」で再接続
3. それでも改善しない場合はアプリを再起動

### ログの確認

エラーが発生した場合、アプリのインストールフォルダ内の `Log` フォルダにログファイルが保存されます。

- 形式: `YYYY.MM.DD.log`
- イベント別: `イベント名_YYYY.MM.DD.log`

---

## 更新履歴

- **v1.0.0**: 初版リリース
- **v1.0.1**: 読み上げ処理修正
- **v1.0.2**: 翻訳変換設定の初期化処理を追加

---

ご不明な点がありましたら、GitHubのIssueページでお問い合わせください。
